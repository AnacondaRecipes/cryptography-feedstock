anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: pyca/cryptography
      use_max_tag: true
      include_regex: ^\d+\.\d+\.\d+$
    packages:
      - cryptography
      - cryptography-vectors
